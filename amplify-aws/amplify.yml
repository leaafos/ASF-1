version: 1
frontend:
  phases:
    preBuild:
      commands: -ls -al
        - npm ci
    build:
      commands:
        - "npm run build"
  artifacts:
    baseDirectory: amplify-aws/frontend/build
    files:
      - "**/*"
  cache:
    paths: node_modules/**/*
